// This Program is called with the main program (External-2).
// Excecutable form of this program is needed with .out fromat.
// this .out file should be copied to folder of main program.
// In order to avoid duplicate .out files with the same name(a.out)
// the name of this excecutable file should be changed to b.out (for e.g.)
// So: mv a.out b.out;

#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>


//char *strrev(char *wrd);


// int main(int argc, char *argv[])

int main(int argc, char *argv[])
{
    
        printf("\nHi\n");
        getchar();

        return 0;
    }
